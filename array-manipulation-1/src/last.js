/* exported last */
function last(array) {
  return array[array.length - 1];
}
// define function named last is being defined with one parameter array
// return the array parameter at index length of the array parameter minus one
