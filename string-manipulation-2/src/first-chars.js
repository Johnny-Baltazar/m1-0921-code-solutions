/* exported firstChars */
function firstChars(length, string) {
  return string.slice(0, length);
}

// this function has two parameters, and returns the specified number (length) of characters
// of the string parameter. The splice() method is used to return the 'length' of characters,
// starting at index 0 thru the number of the 'length' parameter.
