/* exported getLastChar */
function getLastChar(string) {
  return string.charAt(string.length - 1);
}

// define a function named getLastChar which takes one parameter of 'string'
// return the charAt method of the string parameter with one argument, string.length minus one
